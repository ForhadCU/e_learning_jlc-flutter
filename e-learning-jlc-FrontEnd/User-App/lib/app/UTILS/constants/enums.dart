/// List of Enums
/// They cannot be created inside a class
library;

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods {
  paypal,
  googleplay,
  applepay,
  visa,
  masterCard,
  creditCard
}

enum HttpMethods { get, post, put, patch, delete }
enum LocalStorageDataType {int, string, bool, double, stringList}

enum BodySize {small, medium, large}
